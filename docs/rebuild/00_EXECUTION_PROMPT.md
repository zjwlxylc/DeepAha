# DeepAha 机会星图：重建执行提示词

你是本项目的产品负责人、架构师与实施工程师。基于当前用户明确授权，先形成可执行设计，随后直接在隔离副本中开发、验证并打包，不等待下一次指令。

## 目标
交付一个真正使用服务端持久数据的机会产品，不再交付伪装为完整系统的 HTML 演示。核心价值是让青年发现原本容易错过的机会，理解可靠依据、未知与限制，形成收藏、准备、申请、结果反馈闭环。用户端移动优先，审核与系统管理桌面优先，沿用第二版蓝橙界面，正式界面不显示原型、演练、设计说明。

## 决策顺序
当前用户要求 > 2026-09-13/14 WMA整体审核替代规划 > 第二版界面 > 已验收领域约束 > 历史探索性文档。旧文档中只允许规划、逐阶段等待确认、先补Gold或方法审批的安排不适用于本次明确执行委托。公开信息免费；支付、人格测试、全国全量采集仍不抢占主线。

## 必须做到
1. 实际读取源码与依赖，输出保留/重构/退役清单及输入哈希；不凭旧聊天把某功能标为已实现。保护原数据库、原始文件、历史决定、已有稳定ID；不修改远程仓库、不部署到真实服务器。
2. 保留模块化单体与FastAPI、SQLAlchemy、Pydantic技术栈；复用Direct WMA传输、证据存储协议、安全边界、领域四态与V2视觉资产。不重建浏览器/工具调用智能体，不复活Native Automation Worker或旧Provider调查。
3. 先保存原始返回再适配。适配已有WMA三文件格式，支持多机会、子对象、陌生字段、重复标签、复杂条件、附件缺口和冲突。程序不会转换不阻断展示；已证实错误不能作为正确值公开；身份错配等整包不可使用问题一次拒绝。
4. 审核只提交APPROVE/REJECT、精确预览哈希、幂等键及可选整体说明；不通过仅一条原因。决定和总览发布使用同一数据库事务，重复或并发提交不重复发布、不覆写历史。原件及预览不可由浏览器编辑成新事实。
5. 普通用户、审核者、维护者服务端隔离。真实密码登录、会话撤销、CSRF、受限上传、文件路径和HTML安全、无密钥泄露。不要把前端隐藏按钮或localStorage当权限。
6. 总览可分页搜索、详情可按子对象/分区读取、公开仅输出允许字段。未知时间不伪造倒计时；更正/撤回让相关提醒和缓存失效。画像相关排序与资格分开；没有可信规则不能判用户不符合，也不能给假百分比。
7. 提供画像、收藏、行动记录、结构化反馈、消息、可靠时间提醒及基础账户权限。管理仅运行/连接、来源、任务、记录四入口，连接检查显式触发，暂停采集不撤销历史内容，恢复文件不得重跑WMA。
8. 外部Scout仍人工交接，受控导入和去重。来源登记/批准、采集运行、内容收录不能互相冒充。新接口能力以实现和实测为准。
9. 每个关键行为先写失败用例，再实现。至少验证持久回读、局部失败、整包拒绝、恶意上传、越权、重复决定、版本冲突、更正撤回、时间限制、手机页面、实际HTTP前后端联动。只有实际运行的测试可记PASS。
10. 最终打包全部运行源码、原始共享底座、测试、可重现样例、架构/实施/部署/迁移/安全/用户手册及验证记录。列出所有未验证的外部依赖，绝不把预置数据、SDK假件、SQLite测试或浏览器截图称为真实WMA/PostgreSQL/支付/生产验收。

## 执行方式
先建立隔离副本和证据目录，记录所有设计偏离和理由。每次只完成一个可运行闭环，自修复以定位根因为先。工具或外部凭据受阻时继续完成可独立验证的工程部分，不伪造结果，不临时制造网络接口。用户收到的应是可启动的软件与完整交付边界。
