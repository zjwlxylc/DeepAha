# SG6 开发入口

SG6 目的：让 DeepAha 从“一次发现机会”进入“持续抓住窗口并记录真实结果”的闭环。

固定链路：

`收藏 → 准备 → 已申请 → 等待 → 完成/放弃 → 结果反馈 → 脱敏评估候选`

并行能力：

`来源周期 RECHECK → SG3 Currentness → 变化提醒 / 旧截止失效 → SG5.1 新安全截止 → 新提醒`

本阶段 Gate：一个具体机会可以跨版本持续跟踪到变化与结果；旧错误提醒被取消；用户反馈只能进入离线评估资产，不能直接改变硬规则。
