# 后台条件快照查看入口

接续 PR #19；使用已有受信快照服务，增加私有生成 / 读取 API 与桌面优先的后台页面。范围不包括画像、资格结果、范围审批、原件解析扩展或公开页面。

- POST 按当前 RulePreparation / FactPreparation / EvidenceCheck / 身份构造快照；GET 每次调用受信重建读取。未启用环境隐藏入口；鉴权及响应禁缓存沿用私有审核 API，失败不能退回旧 JSON。
- 当前规则候选均已有终局决定后展示生成入口，无可执行规则的未知事实仍能生成条件清单。所有验证仍由服务执行。响应丢失可复用相同输入重试，成功后提供明确查看链接。
- 新查看页展示具体目标与版本、完整条件分母、保留及排除范围、未知/拒绝/未处理、原备注、证据三态和独立批准来源；证据入口复用共同回执及私有原件下载。始终明确这是条件快照，整体资格与完整范围尚未判断。
- 测试覆盖 API 禁用/鉴权/错误/精确请求，以及实际 PG HTTP 薄链路；组件、操作、桌面和移动浏览器丢失响应/重载/过期路径；构建和静态检查通过后按同一候选 CI 合并。

状态 IMPLEMENTED，API、真实 PG 薄链路、前端回归及构建、桌面/手机浏览器与截图检查、独立复核通过；候选 CI 与合并待完成。证据见 `docs/development/2026-09-08-unit-snapshot-review-ui.md`。快照页面读取和源行关联均核对实际版本，不新增任何候选或人工批准。WMA 冻结样本及所有原件保持不变。
