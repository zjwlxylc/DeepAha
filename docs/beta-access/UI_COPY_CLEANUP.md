# 用户界面文案与信息层级调整

状态：**DEPLOYED_AND_VERIFIED**。已人工验收、提交main、推送并部署正式站。本次不改变资格判断、推荐规则或业务数据。

## 修改方案与完成范围

1. 星图删除解释性副标题和“从发现到行动”等宣传段落；行动区仅保留标题、统计及入口。导航“发现”统一改为“首页”。
2. 机会详情以标题、时间、条件和申请入口为主。重复字段备注收进“说明与来源”，公告补充说明、更新记录默认折叠；保留“不采用”、时间冲突和资格待确认等事实状态。审核工作台逐字段证据提醒保持可见。
3. 清理首页、关于、总览、行动、通知、画像、资格/推荐弹窗、共创、账号及管理页面的重复说明与工程术语。精简推荐机制、编译、WMA、Should I等面向用户的解释。共创评估规则用日常语言并折叠。
4. 保留真实使用所需的信息：密码要求、凭据仅显示一次、会话失效、删除后果、数据用途、共创自愿与退出，以及影响申请的具体风险。未删除后台证据和审计数据。

## 验证

- 31个product测试文件逐文件执行：首轮30个通过，1个因固定断言旧文案失败；更新为等义隐私/评估文案后单文件重跑RC=0。原始结果与重跑证据均保留。
- compileall、product/SG7.1/SG7.2前端检查通过。
- 新增字段渲染检查：说明默认折叠、展开内容保留、特殊文本转义、不采用标记、继续阅读、来源链接与审核视图提示保留。
- Chrome合成数据验证：390px移动端星图与详情、资格与推荐弹窗，以及11个用户页面；1440px桌面星图检查。无JavaScript错误，移动端无水平溢出。截图不含真实用户数据。

证据目录：`evidence/ui_copy_cleanup/`。本地代码目录：`D:\DeepAha`。

## 后续验收内容

- 出生日期改为数字键盘输入年份、月份/日期分选，已有值回填、一键清空、日期合法性校验；仍保存YYYY-MM-DD。Chrome移动端编辑、提交与清空通过。
- 本地启动器补充账号结构升级，先执行SQLite在线备份并校验，再补建准入表；现有账号密码保持。新增回归通过，本机实际HTTP就绪验证通过。
- 发布前14项定向测试通过，前端契约、渲染和生日边界检查通过，compileall通过。原业务全回归证据沿用前次同代码结果。

## 正式发布
- 应用提交：`9149e03f00aab99137b6240ac0817f1216213263`。
- 发布目录：`/opt/deepaha/releases/beta-access-20260920-9149e03f`。
- 上一版本：`/opt/deepaha/releases/beta-access-20260920-b35ccd03`。
- 备份：`/var/backups/deepaha/public-beta/beta-access-20260920-9149e03f`；独立空库恢复演练通过。
- 2,282个文件核验一致，宿主注入秘密精确0命中；已有账号密码未变。
- 真实HTTPS注册、权限边界、生日保存/清空、重定向检查通过。合成验收账号停用、邀请码撤销。
- API/Worker active且NRestarts=0；隔离staging未变，无WMA调用。
- 回执：`evidence/ui_copy_cleanup/public_receipt.json`。
- 公网Chrome前端验证通过：实际HTTPS静态资源配合拦截的合成API数据，验证11个移动页面、星图/详情与生日输入，无JS错误。另有上述真实后端HTTPS功能验证。
