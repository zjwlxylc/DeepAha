# Round 1 实施说明

目标：修复桌面用户端行动入口缺失，并让角色入口与后端权限边界一致。

代码改动：
- `user.js`：重构桌面导航；星图并行读取行动列表并显示行动摘要；所有导航写入 `aria-current`。
- `workbench.js`：按 reviewer/operator 分组侧栏，动态工作台名称；用户端返回入口统一。
- `product.css`：新增桌面导航、行动摘要、工作台分组样式；保持移动底栏。

验收：桌面可从任意用户页面进入收藏与行动、消息、我的；reviewer/operator 菜单与权限语义一致；移动端原底部导航保持。
