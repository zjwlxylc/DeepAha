# Codex接续入口

请以本包3.0.0-rc2为候选，不把旧v1导入接口接回研究ZIP。先读README、docs/source-import/00_AUDIT_AND_SCOPE.md、01_DESIGN.md、03_CONTRACT_AND_API.md及05_VALIDATION_AND_BOUNDARIES.md。

先比较用户实际工作树与本包基线，保留用户更改；不能把补丁强行覆盖不同版本。只在隔离库运行上述定向测试。真实数据升级前停止API/Worker、验证备份，SQLite用upgrade，PostgreSQL按宿主备份后init；不要在真人库跑清表测试。

验收核心：原工具v0.3.1导出 → 来源资产上传 → 候选及Brief完整可读 → 一次接收 → 持久回执 → 按需批准来源 → 手工新建调查得到固定Brief → 下载系统反馈 → 原工具读取。没有凭据时停止在任务契约验证，不能伪造WMA成功。

保留手工上传及原工具已有资料库/Codex功能，不新增私有API、全自动扩源、逐字段审批、假官方性、假来源状态、自动发布或多套任务系统。原生浏览器、真实WMA和实际部署单独记录，不把桥接或模拟客户端结果写成这些能力已通过。

本轮无需恢复旧Provider路线。当前机会结果仍然由整体审核收录，来源研究接收绝不意味着机会已公开。
