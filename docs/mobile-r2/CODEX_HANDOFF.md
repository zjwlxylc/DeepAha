# Codex 接续入口：Mobile R2

先阅读 RUN_AND_UPGRADE.md、TEST_REPORT.md、REVIEW.md，以及根目录 CHANGESET_MOBILE_R2.json。

用户已验收并正在发布的是父包 experience-84353c7-20260920。本包只以该父包为基线，未假装包含你正在进行的提交。父包 SHA256：e378d7abbf4f6a23f640eeb730aab6f9b82a42601283a991f4cd6e8fc91d7b60。

等待当前发布收口后，从届时 main 建独立短分支，按差异清单三方接入；保留你的部署修复、配置和文档。先检查源码完整性，再运行 R2 verifier、逐文件回归、原生浏览器检查；不要提前合并、推送或替换正式站。独立审核后再由用户确认 staging 发布。

本轮重点复核：收藏不降级既有进度、准备清单响应丢失不重复、资料冲突与清除后的草稿处理、跨账户隔离、静态资源版本目录、手机弹层与输入键盘。累计数据库回退安全锁继续保留，不新增 R2 migration。WMA 真实调用须另行授权且保持当前保守串行/既有验证上限。

不要把作者自检当独立代码审核，也不要把测试桥的 DOM/HTTP 结果当真实手机或 HTTPS/Cookie 验收。
