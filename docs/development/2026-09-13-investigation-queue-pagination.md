# 调查队列摘要分页（R1-A）

2026-09-13：本地实现及定向验证完成，尚未取得本候选远程九项 CI、合并或真人验收证据。

新增 `GET /api/v1/local-human-test/investigations/queue`，沿用本地审核开关、身份、角色和用途边界。旧列表接口与详情深链保留。新页面服务端筛选 status/source/q，默认每页 20 条，允许 1–100；按 `created_at DESC, task_id DESC` 键集分页，游标绑定规范化筛选、排序版本和首屏上界。游标校验和用于拒绝损坏的导航参数，不是签名或授权凭据；所有请求仍独立鉴权。状态变化不承诺跨请求事务快照，可返回首屏刷新。

查询仅选取摘要列及材料数量，不装配完整 facts/report/delivery/原件，不调用旧 `_view`。未提供无可靠分母的审核完成百分比；页内计数明确标为“本页”。筛选和游标保存在 URL，进入详情后可返回相同队列上下文。非法筛选提供重新查询入口。

本轮实际验证：

- 隔离 PostgreSQL 55437：4 项集成通过，覆盖 101/250 条合成任务、相同时间戳、完整无重复遍历、筛选变更拒绝、空结果和首屏后新增任务；每页捕获到一条 SQL。
- 同一组 4 条合成排队任务：摘要 JSON 2,180 字节，旧列表 5,391 字节。仅为此合成样本，不外推真人任务性能。
- API/游标契约 46 项通过；页面 29 项通过；Ruff、格式、mypy、Web 类型与定向 ESLint 通过；生产构建通过。
- Playwright 桌面 1280px、移动 390px：调查回归首跑 34 项通过，2 项因旧文本断言同时匹配新筛选选项而失败；收窄为状态徽标后，相关注册和队列导航共 4 项重跑通过。队列场景覆盖键盘进入详情、返回筛选、空结果及无横向溢出。

登录交接修复已单独提交 `98dcbbc`；本轮隔离集成 3 项、单元 8 项通过，Ruff/格式/mypy 通过。源工作树五文件逐一移植并比对 SHA-256 相等，源目录和真人服务/数据库未改动。

下一步继续 R1-B 对象化工作台。以上均为工程和合成证据，不代表真人完成两个岗位审核，不提升 Release Qualification 或契约成熟度。
