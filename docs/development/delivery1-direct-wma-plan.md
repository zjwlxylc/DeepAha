# 交付一：受控调查与可信原件回收

本次实现依据 2026-09-07 全面审查的交付一。基线为 `9c8f5b2`，独立分支 `codex/delivery1-direct-wma`。本文件是实施记录，不表示真实调查或发布资格已通过。

1. 修复 Windows / Linux 类型检查，保留文件锁和 DPAPI 行为。
2. 选择性接入 Direct WMA SDK 0.3.4，冻结 SOP / Schema / Prompt 摘要；上传失败不得继续；二进制原件不经过文本解码。
3. PostgreSQL 保存任务、远端标识、状态事件、材料谱系和内部审核。任务登记与真实执行分开；整次执行有绝对截止时间，恢复只回收已有结果，不自动再次调查。
4. 三件套及全部登记原件回收后校验实际 SHA-256、实体绑定和字段证据；未知语义保留人工问题。固定材料 / 子项清单保留分母。
5. 复用现有本地运营鉴权和页面，提供任务登记、候选、原件下载与非合成真人审核。
6. 验证重试、回执丢失、进程中断、超时、坏清单 / 坏哈希 / 路径穿越；运行定向与必要回归、迁移、类型检查和页面构建。

## 边界决策

- 调查交付暂存是摄取候选，不是第二套正式事实权威。内部 APPROVED 表示真人完成本批材料审核；本次不自动写公开目录、资格规则或 VerifiedFactSet。事实质量及 Opportunity / Unit / 规则桥接属于路线中的交付二。
- 原件通过现有 RawArtifact / ObjectStore 入库，collector 明示 Direct WMA，HTTP 状态未知时为空；不伪造 STATIC_HTTP、CaptureObservation 或 P9B Fetch。
- 真实调用必须由运营显式启动，满足本地启用、WMA 配置和调用授权记录。单次调用和时限可限制；SDK 无法提供可执行金额硬上限，不声称已限制货币费用。
- 工程回归使用合成内容和模拟 WMA；后续真实配置、传输和公告检查单独记证据。这些检查均不能代替真人语义审核。

## 本批结果

上述工程工作已形成可审查候选。追加实查发现的发布 Agent 绑定缺陷已修正：当前使用发布会话入口、持久化分步远端标识并核对发布配置；真实连接、二进制回收和新连接恢复已通过。临时全局阻断已被实际配置核验替代。任务输入契约升级至 `direct-wma-intake/2`，两份 Schema 原样保留；系统提示词候选未发布。真实公告、语义审核及正式事实桥接分别记录，不能用连接检查代替。验证明细和剩余工作见 [交付说明](2026-09-07-delivery1-direct-wma-handoff.md)。

WMA 真实调用曾返回模型服务未找到，而 Agnes 直连已成功；两类结果分别保留。用户已明确模型由其在 WorkBuddy 后台固定配置，不由 Codex 负责。主仓不指定/覆盖模型，不新增模型配置或凭据同步工作。下一步继续处理已发布 Agent 接入、提示/交付契约、原件、证据及审核链路。官方速率参考、历史产物可复用范围及未验证项见 [专项核验](evidence/2026-09-07-agnes-api-and-wma-routing.md)。
