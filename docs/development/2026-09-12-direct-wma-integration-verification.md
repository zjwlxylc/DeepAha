# Direct WMA 隔离成果整合与核验

实现状态：`IMPLEMENTED`。最终候选 CI 以 PR #51 的对应提交检查为准。本记录不构成 Engineering Gate 关闭、Release Qualification 合格或独立真人审核。

## 来源与保全

用户确认 `D:\WB-DEEPAHA\DeepAha` 是另一编程工具在原任务暂停期间使用的隔离目录，并授权核验后整合。该目录是在排查启动端口占用时，通过 API/Web 进程启动路径及数据库容器归属发现；未预先假设它是权威实现。

两处待整合代码已分别保存带 SHA-256 清单的工作区外备份。候选目录原文件未修改。两处数据库均以 PostgreSQL 自定义格式备份；当前目录数据库确认没有调查任务后，导入候选数据库副本用于整合验收。候选原数据库保留。原始对象按已有内容寻址存储读取，没有改写。

另一工具停止后才继续独占本机体验端口。发现的旧进程只按已核验的目录、命令与子进程关系停止，未按端口任意结束程序。

## 采用与修正

- 采用候选的页面派发、材料到审核导航、合并单元格读取、不支持格式的显式排除与原件引用；整合后重新测试。
- 发起请求绑定加密配置版本及实际操作人。`dispatch_context` 独立于不可变 `execution`；既有执行证据保护未放宽。
- 恢复请求只回收既有远端会话材料，保留请求键、完成标记与历史。历史任务不因进程重启而自动重试；重复回执不产生新尝试。仍有活动租约时，另一处理进程不能结束它的派发意图。
- 配置替换不会让已授权任务改用新 Agent。历史任务恢复时核对既有 Agent/调用方绑定。新发起请求仍须有有效连接检查。
- 页面区分等待前置步骤、需要处理和已完成。材料准备与候选整理不等于事实批准或完整资格验证。
- 心跳同时核对数据库与代码目录，避免另一工作副本被误报为当前服务就绪。
- 保留真正启动入口的显式验收选项：`-VerifyEntry` 只核对入口与来源；`-VerifyInvestigation` 经浏览器页面登记和发起一次真实调查。普通启动不触发真实调用。验收回执保存在本地运行目录，避免重复创建任务。

## 已检查证据

候选原数据库包含 4 项调查：1 项 `APPROVED`、3 项 `FAILED_VALIDATION`。状态来自当前数据库读取；`APPROVED` 不能单独证明独立人审成立或体验目标全部达成。

三份原始对象的内容、存储元数据和数据库 SHA-256/字节数一致：

| 类型 | 字节数 | SHA-256 |
|---|---:|---|
| HTML | 212888 | `c42e7641660f1fa3c67f277f845d66a30ebcc2d769e372a856e3756c50bac313` |
| XLSX | 2681083 | `a3485e908528dd22a917b5acc83db0760d5412e8598729bd49f54f52bcc1ae38` |
| DOC | 142336 | `3790369304ca83fdae88729127fa2a60fdc8e17b2b6fafeb5f6e98f575ea88c4` |

整合后的定向与相关回归：后端 692 项通过；Web 72 个测试文件、525 项通过；`mypy src tests` 检查 537 个源文件通过。一键入口执行过实际迁移和生产构建。最终候选 CI、真实页面调查结果及人工责任边界仍须在后续交付时更新，不能用上述测试替代。

## 真实页面验收与人工边界

真实一键入口已核对全部 5 个选项及 390px 移动端布局。2026-09-12 经页面登记并明确发起任务 `01a0957a-7e1f-74f9-8a9e-fd657c948953`，输入为用户批准的第一条 zjhr 聚合页。任务回收 3 份材料及 2527 项候选信息，进入 `PENDING_REVIEW`；没有再次发送调查 prompt。


页面已实际执行文档准备：XLSX 生成 3048 处可核对段落/单元格及对应引用；JSON 公告材料和 DOC 指南格式尚未支持，结果为 `1 / 3`、`NEEDS_ATTENTION`。没有为了验收完成而排除这两份文件。材料网址是 `AGENT_DECLARED`；DOC 声明地址还指向 XLSX 名称，需人工核对其真实出处。文件已保存和字节一致均不能证明网址声明或语义正确。

初次页面验收的后续检查未完成；重开同一任务后成功运行到文档准备与人工审核边界。本地回执为 `DELIVERED`，独立审核为 `NOT_PERFORMED_BY_VERIFIER`。截图曾出现关闭/空白问题；另以启动器同一审核身份进行只读浏览器复核，真实页面可见，移动端无横向溢出。不能将空白截图作为视觉验收依据。

现有历史记录只证明曾有关联与字段决定；本次真实任务没有独立真人批准、事实晋升、完整规则审核或范围签署。后续由有职责的真人阅读原件、处理来源疑点并作出决定，Codex 不代签。聚合页与一手原文不合并身份或擅自升级来源等级。

## 审核交互改进

用户指定 [产品化原型工作台](https://deepaha-opportunity-product.ben304994.chatgpt.site) 为参考。通过 Sites 授权接口读取当前版本 22 的页面与发布脚本，采用其中的审核队列、当前对象、原文对照与明确决定结构；不复制演示数量、发布行为、品牌变体或阶段声明。

- 任务列表前置，展示实际待审核、调查异常与本页记录数；可搜索、筛选待处理内容并打开原任务。
- 原文对照每页最多 12 条，条件与其原文保持成对显示，全文关键词搜索可找到后续页；不因筛选删除内容或改变审核状态。
- 当前下一步保留一个直接入口，完整流程可展开；固定导航可到原文、附件、材料决定、字段和资格条件。
- 文件优先展示名称；内部标识、原始问题代码和调查说明放入展开详情。审核与暂不解析操作解释实际效果，不把未知当通过。
- 已进入逐项审核的条件按条展开，表单保持挂载，展开/收起不会清空尚未提交的输入。

本地 Web 73 文件 / 528 项测试通过后，新增真实结构大清单原文对应测试亦通过；完整浏览器回归首轮 108/112，通过定位到表单修正 4 项新增分页状态导致的测试歧义。最终合并仍要求同一候选九项 CI 全部通过及合并树一致，不将早先候选检查冒充最终结果。
