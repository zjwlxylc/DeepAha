# 公告继承条件的派生快照

本批承接 PR #22，把当前岗位基础快照与公告事实、规则批准和精确岗位适用性记录连接为独立派生版本。基础 `unit-qualification/2.0.0` 的内容与消费方式保持原样；新记录使用 `investigation-announcement-snapshot/1.0.0`，仅用于范围审阅，整体资格始终为 `UNCERTAIN`。

派生结果保留完整 `base_v2`，并逐条保留所有公告条件。实际来源事实、规则及批准仍使用原公告身份；`APPLIES` 对应继承，`DOES_NOT_APPLY` 对应有审阅依据的排除，没有决定或尚未完成的来源工作继续待决。事实未知、未推广、字段或规则拒绝、缺少规则准备及待裁决均分别列出，不删减条件分母。单位组、备注、Word 未核验和完整范围审核等既有缺口保留在基础快照中。

服务自行重建全部当前来源，调用方不能选择一组规则来隐藏其余条件。预览返回当前依赖摘要；保存只接受期望摘要，输入有变化时拒绝保存。相同输入并发或丢失响应后重试返回同一记录；任何新决定，包括相同结果的新理由或证据，都产生新依赖版本。旧记录保留，但当前读取拒绝过期内容。

每次预览、保存和读取都核对当前授权、基础快照、事实与规则批准、实际证据关联及适用性历史。数据库迁移 `20260909_0044` 保护真实目标、摘要、完整公告分母、适用记录及来源规则，拒绝 UPDATE / DELETE，有历史时禁止降级。服务还会从实际来源重建完整内容，不能凭写入方自行重算摘要来通过读取。

独立审查复现了一项时间边界缺陷：公告事实的批准日期可以晚于当前时钟，而旧来源重建未检查该时间。新增真实 PostgreSQL 测试通过正常插入事件构造未来决定，未禁用数据库触发器；原行为未拒绝。修复后来源重建拒绝未来批准，同一失败测试转为通过。独立复核未发现剩余 P1/P2。

本批是合成工程验证，不代表独立真人批准或真实资格。冻结 WMA 结果继续为 94 PASS / 0 FAIL / 30 Word UNVERIFIED，Delivery UNVERIFIED；未重新调用 WMA、修改提示词、Candidate Facts、引文、原件或现有资格内核。

状态 IMPLEMENTED，PR #23 已合并，候选 CI #101 九项全部通过。合并提交 `0b0b53e52a1fcd3722f20f113f0d04fedc8a70a9` 与候选 `929a1fa7fa3b197b930692472e77634918fc69a9` 同树；主线 CI #102 成功，证据见 `evidence/2026-09-10-announcement-inherited-snapshots-ci.json`。后端 1454 项非集成测试通过；完整 PostgreSQL/Moto 回归 661 passed / 3 skipped / 0 failed，其中新增 29 项全部通过。3 项既有环境跳过分别需要专用 Phase 6/8 数据库或未挂载的真实来源语料，不计为通过。Ruff、492 文件格式、Linux/Windows mypy（453 文件）、迁移空表往返、有历史拒绝降级及 Alembic 模型差异检查均通过。

整站 248 项测试（42 文件，新增 26 项）、lint、类型检查和生产构建通过。32 个既有浏览器场景通过，新增 12 个桌面/手机场景在修正跳转等待后全部通过。初始 4 次失败源于点击已保存链接后立即刷新，Next 路由尚未切换；测试现明确等待真实记录 href，再执行原保存和过期断言。恢复任务时已确认补丁先于成功结果落盘，只复跑受影响的 4 个场景，全部通过；没有重复整站测试与构建。

8 张桌面/手机截图已逐张检查，包括预览、已保存、丢失回执和过期记录。证据见 `evidence/2026-09-09-announcement-inherited-snapshots-validation.json` 及同名图片目录。原五个持久化合成画像场景已离线重放，完整输出保持一致，所有整体结论仍为 UNCERTAIN。下一批继续既定第 3 项的单位组、原件范围与例外。
