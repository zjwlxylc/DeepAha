# 单位组规则独立审核页面

状态 IMPLEMENTED，本地定向验证通过；PR #32 候选 `ba3a1b4a0b86dec02de3a607a8777f60f1678302` 的 CI #119 全部 9 个作业通过，已合并为 `2fc37ee3d8fab08a93e9a730a933ea7f80cb68af`，候选与合并代码树一致。证据见 `evidence/2026-09-10-group-rule-review-ui-ci.json`。本批为管理页面接入，不代表真实人工审核或发布资格已经通过。

只读预览新增“进入独立规则审核”链接。准备页地址 `/review/investigations/{taskId}/group-facts/{prepId}/rules/review`，明确保存候选后进入 `/review/investigations/{taskId}/group-rules/{preparationId}`。两条路由均动态读取，保留当前完整 GROUP 字段分母和排除项；未知、拒绝、未处理条件没有批准表单。

桌面左侧原文、原件、定位和机械核验，右侧规则与逐引用评估；窄屏堆叠。证据 authority、relation、effective_at、适用范围和说明默认未评估，APPROVE 必须全部明确。支持拒绝、待裁决、独立审核历史和原件回链；不增加岗位继承、UnitRuleSet 或资格放行。公开用户端与首页没有改动。

服务端动作核对准备摘要、当前预览、精确组身份、事实集、候选与引用集合以及全部审核历史。POST 后重新读取当前记录，并核对实际提交的决定、说明及证据评估；时间表示只按同一时间点比较，不模糊匹配文本。以精确请求生成稳定幂等键；丢失回执时隐藏旧内容，只允许明确重试原请求，权限/过期/不可达状态均不显示旧表单。

首次保存后持续隐藏并锁定旧准备页，直到保存地址页面挂载。独立评审发现原实现会在 router.replace 尚未完成时提前开放审核，导致较早的 RSC 响应覆盖新决定；组件用例先复现失败，再修复。浏览器额外截住已取回的目标 RSC 响应，证明等待期间旧页无字段表单、操作按钮禁用，放行后才进入已保存审核页。

验证：49 个定向 Vitest 测试通过（新动作 17、新组件/保存路由 8、原动作 17、原预览组件/路由 7）；相关 12 文件 ESLint、TypeScript 及新增路由生产构建通过。最终 Playwright 32 项通过（新审核 12、原预览 8、原组字段 12），桌面 1280×900 和窄屏 390×844；包含保存地址确认后刷新、显式批准、全部字段、过期/权限/服务失败隐藏及恢复、丢回执重试和延迟导航。批准路径 POST 2/变更 2；丢审核回执后重试 POST 3/变更 2；只读预览保持 POST 0/变更 0。

首次浏览器运行发现测试 getByLabel 精确匹配未匹配含选项的标签；可访问性快照确认正确 combobox 名称，改用角色与名称定位后通过。未改业务行为以迎合该断言。导航修复后重新构建并运行最终 32 项，未重复整站单元测试。桌面与窄屏截图保存于 `evidence/2026-09-10-group-rule-review-ui/`，无横向溢出或遮挡。

独立复核确认导航 P2 关闭，无剩余 P1/P2。验证回执与源码 SHA256 见 `evidence/2026-09-10-group-rule-review-ui-validation.json`。所有浏览器数据、审核账号和决定为合成机制验证，不是真实人工批准。没有 WMA 调用、官方下载、后端或冻结交付修改；实际样本保持 94 PASS / 0 FAIL / 30 Word UNVERIFIED，Delivery UNVERIFIED，整体资格 UNCERTAIN。

## 安全恢复点

先确认本批精确候选的全部 CI，成功后合并并核对候选与合并代码树。后续是 GROUP 规则对 POSITION 的显式适用裁决、例外与冲突处理，需要重新核对已有 applicability 契约、证据精确作用域和资格回放；应在该跨层工作开始前切到“高”档。不要从组规则已批准推导岗位自动继承，不改变实际样本的 UNVERIFIED，也不为方便测试重新运行 WMA。沿用当前任务窗口和工作区。
