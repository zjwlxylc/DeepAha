# 单位组规则预览接入

状态 IMPLEMENTED，本地定向验证通过；候选尚待 CI。当前分支 `codex/group-rule-preview`，基于主线 `548023ce30dfc562262eb2ec6795459693ae064c`。上一批组字段管理页 PR #28 的候选 CI #111 已全部通过并同树合并，PR #27 的主线 CI #110 也已全部通过。

新增私有 GET 规则预览接口及版本化响应。重新验证组字段回执和当前 ACTIVE 事实集后，按每条原始组字段输出预览；正式 KNOWN 事实复用 deriver 1.0.1，未知、冲突、拒绝、未处理及不可执行元数据保留状态与依据。回执包含完整组字段审核结果、其他层级排除索引和精确组身份。没有规则写入、规则批准、岗位适用裁决或资格放行。

本地验证：21 个 PostgreSQL 定向测试通过（本批 19 个、共享 fixture 默认行为回归 2 个），新 9 个 API 测试及既有 11 个组字段 API 回归通过，共 41 个不同测试；6 个相关文件 Ruff 检查、5 个文件 Mypy 通过。最初测试因模块尚未实现而失败；首次集成样本误用两条同名学历字段同时晋升，被既有 P9B 拒绝，改用独立户籍未知字段后通过，未放松事实集唯一性。初次 Mypy 指出响应构造参数类型不符，改为显式 Pydantic 验证后通过。交错测试从定义模块显式导入 helper 后，Mypy 和该测试再次通过。未重跑整站前端测试或生产构建，本批没有前端或迁移变更。

独立评审指出 READ COMMITTED 的读取窗口：首次完整性检查后，另一事务合法追加事实证据，后续查询可能混入不同候选的引用。已在全部实际事实/证据读取后增加最终物化校验；用两个真实连接、全部约束启用的交错测试证明修复前未拒绝、修复后拒绝。最终组装响应不再读库，静态复核未发现剩余 P1/P2。没有扩大为旧 P9B 表约束或其他入口的全局修改；本次结论只覆盖新预览链。

本批独立代码复核及最终验证证据保存于 `evidence/2026-09-10-group-rule-preview-validation.json`。实际 WMA 样本、原始文件和 CandidateFacts 未改动，没有实时模型调用或官方下载；仍为 94 PASS / 0 FAIL / 30 Word UNVERIFIED，Delivery UNVERIFIED，整体资格 UNCERTAIN。

安全恢复：先检查本批精确候选 CI，通过后合并并核对代码树。接着用“中”档完成桌面优先的只读预览页面、相关服务端回执检查和定向浏览器测试；沿用当前任务和工作树。若进入组规则持久化、跨层适用或例外继承裁决，重新评估复杂度并在必要时保存后请用户切回“高”。这是按任务边界作出的开发判断，不是不同推理档位质量已做对比实测的结论。
