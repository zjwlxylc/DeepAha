# 可信岗位资格快照接入

本批从当前调查回执、正式岗位版本、ACTIVE 事实集和独立规则决定构造不可变派生快照。生成与读取都重新核验共同证据及上下文，并重建完整计划逐项比对；仅存了 JSON 或重新计算摘要不能成为可信输入。快照不是新增事实、人工批准或资格结果。

新增单个 `investigation_unit_plans` 表（0042），绑定真实 RulePreparation / UnitVersion UUID，保存计划、构造上下文、摘要及操作者。SQL 保护当前身份、摘要及不可变历史；完整内容由受信服务重建校验。缓存命中不能绕过这一步。过期快照的受信读取拒绝执行；本批未提供历史浏览或 HTTP 入口。

全部原始行均计入构造分母：当前岗位、公告和本组条件保留；别岗与别组逐项记录排除原因及原行摘要，不能借用事实或证据。未知、冲突、不支持、未定位、尚未处理和人工拒绝分别保留；拒绝规则不会变成“不适用条件”。原始备注、未解决引用和 PASS / FAIL / UNVERIFIED 各自保留，未核验不能计入通过或错误。

所有规则候选必须已有终局 APPROVE / REJECT；待裁决不能生成快照。准入取实际 P9B producer、独立人工决定及证据评估。审核时间取 P9B 批准时间；回执创建时间独立保存，以兼容现有两次取时产生的合法微秒差。有效期起点来自已保存的官方证据评估，终点 null 表示尚未确定，不表示永久有效。

完整范围和例外审核尚未实现。计划保留 `SOURCE_COMPLETENESS_UNVERIFIED`，离线核心继续保留 `HUMAN_SCOPE_REVIEW_UNVERIFIED`，不能输出确定的整体资格。当前批次不接个人画像，不生成 Match，不改变公开页面。

已完成验证：

- 26 项真实 PostgreSQL 合成测试通过（40.41 秒），包括完整九行源分母、并发相同快照、丢失响应重试、未知/未定位/拒绝、原备注、证据三态、外部任务读取、账号撤销、事实依赖过期、摘要和身份篡改、重新计算摘要后的内容篡改、直接 SQL 写入后受信读取拒绝、不可变历史及带历史降级拒绝。
- 独立评审发现微秒时差兼容 P2，递增时钟在修复前实际失败，修复后通过；独立内存复核确认合法时差通过、审核员不一致仍拒绝。指定范围内无剩余 P1/P2。
- 全后端非集成回归 1414 passed / 605 deselected（53.14 秒）；Ruff / 格式及 mypy Linux、Windows（442 files）通过。
- 全 PostgreSQL / 对象存储回归 602 passed / 3 skipped / 1414 deselected（584.64 秒）；新快照定向测试无跳过。空历史 0042 → 0041 → 0042 迁移往返通过，`alembic check` 无结构漂移。候选 CI 尚未运行。

实现状态 IMPLEMENTED，合并检查进行中。合成审核身份与数据仅用于工程验证，不证明实际人工批准、Gold 或发布资格。未调用 WMA 或官方网络；冻结样本仍为 94 PASS / 0 FAIL / 30 Word UNVERIFIED，Delivery UNVERIFIED。
