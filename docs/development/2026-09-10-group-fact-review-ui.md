# 单位组字段审核管理页面

状态 IMPLEMENTED，本地定向验证通过，PR #28 候选 CI #111 的 9 项作业全部通过并已合并。候选 `2eb49ce` 与主线合并 `548023c` 的树相同；后端 PR #27 的 main CI #110 也已确认全部通过。精确 SHA、树与 CI 回执见 `evidence/2026-09-10-group-fact-ui-ci.json`。后端的原件、冻结 Schema、字段事实和规则均未修改。

已登记组来源新增“进入组字段审核”。准备页为 `/review/investigations/{taskId}/group-bindings/{recordId}/facts`；保存后使用固定地址 `/review/investigations/{taskId}/group-facts/{prepId}`。两个页面强制动态读取，GET 不自动写入。桌面原文/证据与审核表单并排，窄屏自然堆叠，原首页及公开页面保持不变。

准备仅提交当前来源摘要和证据回执。独立裁决只提交决定、证据支持、优先级核查与理由，不允许修改候选值。UNKNOWN 不提供批准为已知的选项；未核验/不支持字段继续显示待处理，不提供裁决按钮。全部可审核候选完成终局裁决，且至少一项获批或保留未知后才显示保存入口。保存事实集不宣称组规则、岗位继承或完整资格已批准。

服务器动作核对精确组身份、当前回执、原始字段与完整分母、证据对应和返回记录。成功写入后重读具体记录；没有相符裁决/事实集回执则不报告保存成功。失败时隐藏旧内容，只有回执不可达时保留原请求供明确重试，同一请求使用稳定幂等键；不自动提交或自动刷新旧裁决。

只读复核发现旧任务展示层会补充 `note`，与冻结原始对象不完全相同。已通过失败用例复现并修复：原始事实缺少备注时只忽略展示层新增的该属性；原有备注变化、其他字段变化、错误分母和证据仍拒绝。排除行摘要仅接受展示原样或缺失备注这两种已知表示。原始回执与摘要不被重写。复核未发现剩余 P1/P2。

验证：43 个不同定向测试通过（新动作 22、组件 7、路由 2、既有组来源组件/路由 12）；类型检查与 14 文件 ESLint 通过。新路由生产构建通过；备注兼容修复后做了一次必要增量构建，没有重跑整站单元测试。

浏览器：桌面 1280×900、窄屏 390×844，共 24 个不同测试通过。初次 8 项未知限制/失败隐藏通过；两项完整流程因 getByLabel 精确文本定位未找到下拉框而失败，按实际无障碍树改用 combobox 名称后两端流程通过。备注修复后定向批次 16 项通过，覆盖两端完整流程、旧备注兼容和已有组来源 12 项回归；前面未受兼容修复影响的 8 项证据复用。所有已保存地址均等待 href 导航完成后再刷新。准备及事实集回执丢失场景证明 6 次 POST 只产生 4 次变更。桌面/窄屏审核和保存后共 4 张截图已检查，无遮挡或水平溢出，见 `evidence/2026-09-10-group-fact-ui/`。

数据和审核账号均为合成工程场景，不是实际人工批准。真实案例仍为 94 PASS / 0 FAIL / 30 Word UNVERIFIED；Delivery UNVERIFIED，整体资格 UNCERTAIN。未调用 WMA，未下载官方原件。

原恢复点已完成：本批 PR 按精确候选 SHA 合并并核对同树，后端 main CI #110 通过。下一步已按风险拆分为只读组规则预览、预览页面、独立规则审核、逐岗位适用范围与继承；当前进展与安全切换点见 `2026-09-10-group-rule-preview.md`。组事实集保存不自动批准其中任何规则或岗位继承。
