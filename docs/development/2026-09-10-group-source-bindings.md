# 单位组来源关联：完整成员与稳定身份

状态 IMPLEMENTED，PR #25 已合并为 `ee96b8a2d2b7bc49ca330e532ae9444584c50ffa`；候选 CI #105 九项通过，合并与候选同树，主线 CI #106 成功。证据见 `evidence/2026-09-10-group-source-bindings-ci.json`。管理页面在下一批单独验证，见 `2026-09-10-group-source-ui.md`。承接已合并的 PR #24，不改变现有岗位绑定、字段映射、规则、v2 资格快照或公开入口。

`group-identity/1.0.0` 用严格的请求、来源、成员、组身份、预览及回执模型表达 `GROUP_SOURCE_ASSOCIATION_ONLY`。三个私有接口为 `GET /{task}/group-source-input?entity_id=...`、`POST /{task}/group-bindings` 和 `GET /{task}/group-bindings/{id}`，均位于既有调查审核 API 前缀下，返回 `private, no-store`。保存只接受 entity_id 和预览的 expected_source_hash；不接受自选成员、组 UUID 或 complete 开关。

服务按冻结交付的完整组层级枚举成员，与当前岗位绑定关联，未登记成员保留 UNPROCESSED；空组为 NO_MEMBERS。组的原名称、编号、嵌套 parent_id 及原字段均保留。BOUND 只表示身份已关联，不表示证据、条件或资格已经批准。

组使用独立稳定 ID 和确定性 task/entity 键。重复保存、并发请求和丢失回执重试复用同一记录；绑定更正后，旧记录拒绝作为当前来源读取，重新关联复用组 ID 并追加版本。每次读取和重试重验当前操作者、来源政策、原件及派生材料、当前机会和岗位版本。数据库迁移 `20260910_0046` 保护完整来源、真实成员、组身份、追加序列和不可变历史，有来源记录时拒绝降级。

独立检查发现的两项 P2 已修复：

- **跨组借用身份。** 原候选只检查同 task/entity 的历史；真实 PG 测试通过合法别名窗口、版本追加和新键构造第二组借用第一组 UUID，未禁用触发器，原版 DID NOT RAISE。新 SQL 在持有 Unit 行锁时检查全部历史归属，读侧同样复核。相同测试转为通过，拒绝后事务回滚，原组仍可完整读取。
- **合法空父级被拒绝。** 冻结交付契约允许嵌套组 parent_id=null，由 evidence 的实际父级表达关系。新关联层原先过严；现沿用原契约并保留 null 原值。纯失败样本及真实冻结、登记、读取、重试生命周期均通过。

定向验证为 70 项相关 API/成员/契约测试通过，以及 22 项 PG 测试通过；分别包含 29 和 20 项本批新增测试。另对原调查完整响应保持不变的新增断言复跑 2 项，通过。迁移往返、模型差异及历史拒降级包含在 PG 测试中；12 文件 Ruff/格式和 11 文件 Linux/Windows mypy 通过，独立复核无剩余 P1/P2。原件缺失测试按数据库记录定位具体原件，初稿任取临时文件的测试方法已纠正，不把测试选错文件算为生产缺陷。

证据见 `evidence/2026-09-10-group-source-bindings-validation.json`。本批没有前端改动，没有重复本地整站 248 项测试与生产构建，也没有调用 WMA。全部批准身份均为合成工程场景；真实案例仍是 94 PASS / 0 FAIL / 30 Word UNVERIFIED，Delivery UNVERIFIED，整体资格 UNCERTAIN。

恢复顺序：先确认本批候选 CI；通过后按候选 SHA 合并并核对同树。随后接桌面优先的组来源管理页面，复用这些接口并验证保存、丢失回执、过期和权限撤销；保持用户首页与手机优先设计不变。组事实和规则仍在管理入口完成之后独立接入，不提前移除范围或 Word 阻塞。
