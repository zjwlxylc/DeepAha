# 交付二：调查材料到事实审核的渐进接线

依据 2026-09-07 已接受的后续开发方案及用户持续执行授权。起点为交付一候选 `4bd5847`，继续使用现有隔离工作区；不改模型配置，不将工程测试当真人事实审核或发布资格。

## 已核实的接缝与决定

调查原件已保存为 `RawArtifact`，但没有 `Document / DocumentBlock / EvidenceRef`。现有 `FactLifecycleService` 又要求冻结的 `SourceBundleRevision` 和正式 Opportunity/Unit 归属；`BundleMemberSpec` 当前强制采集观察、运行和评估标识。不能为适配 WMA 伪造这些记录，也不能把 Agent 的实体 ID 当正式公开 ID。

按依赖先补原件解析，再增加真实 WMA 来源谱系及人工确认的实体绑定，最后接逐字段审核和规则。各片段独立验收；WMA 外部故障不阻断本地工程，但真实校准保持未通过。

## 当前片段：原件到文档证据准备

- 在现有调查详情提供显式“准备文档证据”，限定运营角色，绑定当前交付摘要；不调用模型或访问官网。
- 仅处理已经回收的 `PENDING_REVIEW / APPROVED` 交付。先重核原始包及全部原件，再使用现有 P9-B HTML/PDF/XLSX/DOCX 解析器和 `DocumentService`。
- 每份原件按原件身份、解析器及契约版本复用不可变 `ParseAttempt`。每个材料独立提交；中途失败可再次准备，已提交解析不重复生成。并发操作串行化共享原件。
- 详情读回逐材料解析结果、版本、证据块数。`.doc`、图片等不支持格式明确待处理；失败、低置信和未准备不显示为全部成功。重读状态不触发解析。
- 原始交付与审核决定不变，不新增第二套事实表，不生成 SourceBundle、Opportunity/Unit、正式事实或规则。此片段仅完成后续事实桥接的文档证据前置步骤。
- 验证真实 PostgreSQL、对象存储、API 和浏览器合成链路；覆盖篡改、旧摘要、未交付、退回、无权限、重复/并发、不支持格式及解析失败。既有文档解析测试可按实际改动复用。

## 后续片段

1. 用版本化迁移补充真实 WMA 材料谱系，保持旧采集谱系及冻结摘要可回放；不放宽到任意无来源证据。
2. 提供人工确认的 Opportunity/Unit 归属及候选字段映射，保留公告通用条件、岗位例外和不支持字段，不通过字符串相似度自动批准。
3. 接现有独立真人逐字段审核、事实集晋升、规则编译及资格四态；用完整材料与子项清单评估覆盖、弃权和错误。独立样本与人工真值另行记录，不用模型自评代替。

当前片段完成不代表以上后续片段、交付二整体或真实 WMA 调查已完成。
