# 实际验证记录

交付日：2026-09-17（北京时间）。容器测试日志UTC日期为2026-09-16。以下结果只描述本轮实际运行范围。

## 最终自动测试
`evidence/31_final_test_run.txt`：**90 passed, 2 skipped**。

其中新产品测试49项，复用本地对象存储和WMA传输相关测试41项通过。两个跳过均由于没有安装 `cloud_agent_sdk`：一个发布绑定相关模块、一个实际SDK构造测试。旧测试需要的httpx2名称由显式测试兼容别名指向当前安装的httpx0.28.1；这是测试适配，不是实际腾讯SDK已运行。

本次没有运行原仓所有测试，没有连接真人库，也没有把旧历史CI状态累加为本轮通过数。

## 已覆盖的关键行为
原始文件不可覆盖与读取哈希、输入路径/数量/压缩比安全、重复导入、正常及局部引用缺口收录、核心来源错配阻断、整包可用对象与局部排除对象分开、原文/未知字段/重复标签/深层对象保留、结构化私密键不公开；精确预览哈希、重复回执、相反决定并发、过期预览、新版本替换/撤回及旧日期提醒失效。

账号角色隔离、登录失败限流、会话撤销、CSRF/越权接口、个人画像/收藏/行动/反馈隔离、消息到期分页与关闭通知；来源暂停不撤回历史、外部资产人工导入不派任务、周期检查绑定维护账号；恢复只回收文件不再次Prompt；没有凭据不领取任务。受控旧机会ID关联不批准内容。SQLite备份/完整性校验/新目录恢复实际测试通过，并撤销旧会话。

## 前端和真实接口
JavaScript语法及无原型业务状态检查通过：`30_final_frontend_check.txt`。当前Python模块编译和实际OpenAPI的53个唯一operationId检查通过。

`13_browser_visual_bridge.json`、`28_final_browser_bridge.txt`：**13项检查、16次真实HTTP请求、0个页面脚本错误**。包括登录与数据库队列、整体通过后总览回读、收藏与个人行动、360/390/430/768宽度总览无横向溢出、个人页面及四个管理页面。截图在 `evidence/screenshots/`。

重要限制：浏览器对本机地址的原生导航返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，原始错误见 `29_native_browser_attempt.txt`。上述桥接明确用容器HTTP把界面操作转给真实运行的API，**没有伪造业务响应**，但它替换了浏览器网络传输和路径导航，因此**不能验证原生Cookie、CSP、导航或部署网络**。没有把它称为原生端到端通过。已提供 `tests/e2e/native_browser_check.py`，供有正常浏览器访问权限的隔离环境继续验收。

## 启动与停止
`34_launcher_lifecycle_results.txt`：Linux真实拉起API和Worker，HTTP就绪、状态查询、受控停止、启动器退出码0和运行清单清理通过。测试发现并修复了计划停止被误报为API故障的行为，失败记录在 `33_launcher_red.txt`。

该测试以已安装依赖通过显式.pth测试环境复用并核对版本，**没有联网重新pip安装**，不是Windows双击或首次交互安装验收。提供的专用虚拟环境不进入下载包。

## 仍未完成的验证
| 范围 | 状态 |
|---|---|
| 当前用户已有真实WMA三文件、真实两岗位回放 | 未取得文件，NOT_RUN |
| 腾讯已发布Agent实际调用、成本、文件取回与恢复 | 无凭据/SDK，NOT_RUN |
| PostgreSQL副本迁移、PG事务并发、Docker构建 | 无对应实例，NOT_RUN |
| 原生浏览器网络、Cookie、CSP、实际Android/Windows、200%缩放 | 原生受限或无设备，NOT_RUN/BLOCKED |
| 当前用户原数据库备份、迁移和生产回退 | 未访问原库，NOT_RUN |
| 微信/短信/邮件/支付接口 | 未实现本轮对接，NOT_CONNECTED |
| 真实用户价值、资格准确率、AI匹配质量 | 未验证，不宣称商业成功 |
| 大规模性能、P95与持续运行 | 未压测，无吞吐承诺 |

## 复现
安装 `requirements-test.txt` 后运行 `python -m pytest tests/product -o addopts= -q`。需要复用模块回归时，参考31号日志的显式兼容条件；不能把SDK跳过项改成假通过。原生浏览器脚本要求显式 `E2E_ALLOW_WRITES=1`、本地隔离地址、测试账号以及待审虚构样例；不要拿真人库运行它。

当前样本与测试数据全部为软件验收用途，不自动进入新安装。所有历史失败日志均保留为修复过程，不代表最终仍失败；最终状态以31、28、29、30、34号记录及本文件为准。
