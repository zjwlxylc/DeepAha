# DeepAha 项目 Agent 工作约定

本文件适用于 `D:\DeepAha` 整个项目树。除非用户在当前任务中明确覆盖，否则所有 Agent、自动化脚本和代码生成任务都必须遵守。

## 1. 沟通与执行原则

- 尽可能使用中文沟通；首次出现行业术语时采用“中文（English）”写法。
- 编码前先说明假设、范围和可验证的成功标准。存在会改变产品含义的歧义时，不得静默猜测。
- 采用能解决当前问题的最小方案：不预做未来功能，不为一次性需求增加抽象，不顺手重构无关代码。
- 每一处改动都必须能直接追溯到当前任务、Blueprint 或已确认的设计决策。
- 修改现有代码时匹配已有风格；只清理由本次改动造成的无用导入、变量、函数或文件。
- 功能和缺陷修复优先先写可复现的测试，再实现最小代码使其通过。
- 完成前必须运行与风险相称的验证，读取实际输出后才能声称“完成”“修复”或“通过”。
- 当前仓库只有规划文档和设计参考图。文档中的架构、指标、路线图均为规划基线，不代表已经实现、部署或验证。

## 2. 决策来源与优先级

发生冲突时按以下顺序处理：

1. 用户在当前任务中的明确要求。
2. 本文件中的项目级约束。
3. `文档2/DeepAha_青年机会智能系统_Blueprint_v1.2.docx`，自 Phase 2 起的当前产品、业务和验证策略基线。
4. 已关闭 Gate 的版本化契约、迁移、测试和验证证据；新 Blueprint 不追溯改写历史事实。
5. `文档/DeepAha_青年机会智能系统_Blueprint_v1.0.1.docx`，Phase 0–1 的历史基线，以及 v1.2 未改变内容的补充参考。
6. `文档/青年机会雷达_全面实施与商业化方案_v1.0.docx`，实施、数据源和商业假设补充。
7. `文档/青年机会雷达创业可行性研究报告 v2.0.docx`，市场、竞争、成本和 Gate 假设补充。
8. `文档/青年机会雷达_竞争格局深度分析与进入战略研究报告_v1.0.docx`，战略背景补充。
9. `文档2/DeepAha_创业投资人视角审查_v1.0.docx`，仅作为风险和验证假设输入，不是软件需求或已证实结论。
10. `文档2/DeepAha Opportunity Intelligence Engine 技术架构设计 V1.0.md`，仅作为技术候选输入；不能覆盖阶段 spec、已验证代码或开源准入结果。
11. `文档/DeepAha_青年机会智能系统_Blueprint_v1.0.docx`，仅作为历史版本。

补充规则：

- 新版本优先于旧版本，明确决策优先于探索性建议，项目事实优先于外部通用最佳实践；具体晋升与差异见 `docs/superpowers/specs/2026-08-21-blueprint-v1.2-baseline-reconciliation-design.md`。
- 市场规模、竞品功能、价格、法律法规、模型和云服务等会变化；用于决策前必须重新核验当前权威来源。
- 不得把报告中的示意测算、内部定价、模拟结果或成功阈值写成已验证事实。
- 若文档与已存在的可运行代码不一致，先记录差异并确认迁移方向，不得用文档直接覆盖代码行为。

## 3. 项目定义

### 3.1 一句话定义

DeepAha 青年机会智能系统持续监控权威公开信息，把分散公告组织成可追踪的机会实体（Opportunity）；结合个人状态给出可解释的资格判断、行动优先级和变化提醒，并把真实反馈沉淀为可验证的学习资产。

### 3.2 品牌与运行模式

- `DeepAha`：母品牌。
- `青年机会雷达`：面向用户的第一产品入口。
- `青年机会雷达实验室`：同一系统的验证运行模式，不是另一套软件，也不是一次性演示。
- 长期方向：个人机会情报基础设施（Personal Opportunity Intelligence Infrastructure）。

### 3.3 产品承诺

用户不需要每天到处找。系统知道用户的必要条件，持续盯住权威来源，只在真正相关且需要行动时提醒，并能说明判断依据。

### 3.4 核心价值闭环

所有功能必须改善以下闭环中的至少一环：

`可信发现 -> 判断 -> 行动 -> 学习`

如果一项功能不能说明它改善了哪一环，就不进入当前阶段。

## 4. 当前阶段与范围

### 4.1 当前判断

项目处于“有条件立项 / 验证”阶段。当前目标不是建设全国青年机会平台，而是先证明可信资格、未知机会发现和真实行动可以同时成立。

### 4.2 首发范围

- 架构按全国可扩展设计；运营采用“浙江地方机会深覆盖 + 全国中央级机会”。
- 首发用户必须在未来 90 天内存在报名、申请、材料准备或政策权益申领决策。
- P0：事业单位公开招聘、中央/地方国央企校招、基层服务项目、青年就业与人才政策权益，以及许可与证据边界清晰的比赛、科研计划、奖学金和青年成长计划。
- P1：国考/浙江省考保证基本完整但不作为差异化中心；可信培训和精选实习只做经来源审核的候选；推免/夏令营/招生变化后置到第二阶段。
- P2：普通民企全量岗位不进入首版。

### 4.3 当前最小验证闭环

先完成并验证：

- 200 个真实 Gold 机会样本及公开可信字段。
- 同一数据契约驱动的只读公开机会索引。
- 20 个母画像和 100 个版本化模拟青年画像。
- 20–30 名真人设计伙伴。
- 可回放的资格、覆盖、排序和变化评估。
- 从官方原文、资格解释、行动到结构化反馈的薄链路。

这些是计划产出和验收目标；除非存在可检查证据，不得写成已完成。

### 4.4 首版明确不做

- 全国无边界信息聚合、无限公告流和以公告数量制造焦虑。
- 题库、课程、直播课、培训导流和社区信息流。
- 普通民企全量职位库、招聘交易、代报名、代投递或账号托管。
- 让 AI 替用户作重大人生决定的通用聊天机器人。
- 复杂简历制作、面试训练、原生 App、Kubernetes、自建 GPU 集群或自训基础模型。
- 广告驱动流量、未标记商业置顶，或用付费合作影响自然排序。

## 5. 不可破坏的领域不变量

### 5.1 核心链路

`Source -> RawArtifact -> Document -> Opportunity -> Version / Event -> RuleSet -> Match -> Action`

- `Document != Opportunity`：网页、正文、PDF、Excel、附件、更正和延期可能属于同一个持续演化的 Opportunity。
- Opportunity 必须有稳定公开 ID；合并后保留别名和历史引用，不复用旧 ID，必要时支持受审计的拆分/撤销。
- 原始证据不可被解析结果覆盖。所有高影响事实必须能定位回原文、附件页码、表格行或等价证据位置。
- 机会、规则、用户状态、匹配结果和评估结果都必须版本化，以支持复现、回放和差异分析。

### 5.2 资格与推荐必须分离

资格只允许四态：

- `ELIGIBLE`
- `LIKELY_ELIGIBLE`
- `UNCERTAIN`
- `INELIGIBLE`

硬规则：

- `INELIGIBLE` 必须由至少一条确定性冲突规则和官方证据支持。
- 信息缺失、公告模糊、证据冲突或仅有模型语义推断时，最高只能进入 `UNCERTAIN` 或人工审核候选。
- 排序可以学习偏好和行为，但永远不能覆盖硬资格裁决。
- 不得用“92% 匹配度”等单一百分比掩盖资格、偏好和不确定性的差异。
- 专业匹配优先使用公告指定目录、专业代码和人工批准映射；向量和语义模型只做候选召回。

规则冲突优先级固定为：

`最新官方更正 > 正式岗位/政策附件 > 原始正式公告 > 官方 FAQ/指南 > 人工批准映射 > LLM 语义推断`

### 5.3 反馈不是自动真相

反馈采用受治理流程：

`原始反馈 -> 授权与身份检查 -> 证据关联 -> 置信度 -> 审核/裁决 -> 标签资产 -> 离线评估 -> 规则/提示/排序候选更新 -> 影子测试 -> 发布门`

- 隐式行为只能影响软排序候选，不能改变硬资格。
- 用户纠错不得直接在线修改规则。
- 真人高质量证据和人工裁决优先于模拟结果。
- 原始反馈、批准标签和发布版本必须分离并保留审计关系。

### 5.4 模拟与真人承担不同证据责任

- 模拟画像用于覆盖测试、边界测试、反事实实验和排序校准。
- 真人用于验证理解、信任、认知负担、真实价值、行动和付费。
- 不得用模拟点击率、留存率或付费率形成商业结论。
- 不得让同一个模型同时生成画像、生成真值并给自己评分。
- 模拟画像、隐藏真值、期望机会集合和场景时钟必须版本化。

## 6. 产品体验边界

### 6.1 双层入口

- 公开机会库 / 观测站：回答“系统掌握什么，是否新鲜可信”。
- 个人行动台：回答“哪些机会值得我现在行动”。

公开机会卡至少展示：

- 机会名称、类别、地区、发布单位和稳定 ID。
- 当前状态、发布时间、截止时间和 DeepAha 最后核验时间。
- 官方入口、附件和关键条件证据。
- 更正、延期、附件替换等变化标记与历史。
- 非个性化适合人群概述和进入个人判断的入口。

个人机会卡必须回答：

1. 这是什么机会，官方发布者是谁？
2. 用户为什么可能符合，哪条条件仍不确定？
3. 为什么现在值得处理，截止或变化是什么？
4. 下一步做什么，官方入口在哪里？
5. 判断有误时如何纠错，并如何查看处理状态？

### 6.2 终端职责

- 响应式 Web/PWA：公开发现、搜索传播、可信字段、变化时间线和首次机会体检。
- 微信小程序：个人判断、今日行动、计划、提醒、反馈与分享；不复制网站的信息密度。
- 原生 App：后置，只有真实留存、通知或系统能力证明 PWA/小程序无法满足时才立项。

### 6.3 视觉与品牌使用

- `设计/logo.png` 是当前正式品牌基准：蓝橙灯塔/A 图形、`DeepAha` 字标，以及宣传语 `Go Deep. Find the Aha.`。品牌使用必须保持名称大小写、宣传语文字与标点、图形关系和整体比例一致。
- `设计/` 中除 `logo.png` 外的图片仍是 AI 生成的方向参考，不是生产资产、设计系统或已经批准的品牌规范；其中出现的其他 Logo 方向不得覆盖 `logo.png`。
- `logo.png` 是品牌内容与构图的权威源，但当前文件是带大面积白底和生成平台水印的光栅图，不应原样充当 favicon、App Icon 或页面 Logo。生产使用前应基于它制作干净裁切、透明背景和矢量版本，并保持与原基准的视觉一致性。
- 可复用的共同语言是：可信蓝/深海蓝为主体、橙色用于 `Aha` 与关键发现高亮、充足留白、清晰卡片，以及灯塔/路径/光点等“深入后发现”的隐喻。
- 精确色值、字体、Logo 安全区、图标系统和插画风格应在专门的品牌决策中形成可检查的设计令牌（design tokens），不得从光栅海报中猜测。
- 视觉表达应突出来源、状态、核验时间、变化和下一步行动，不用炫技、信息洪流或虚构匹配百分比代替可信度。
- 原型图中的文案、机会类别、数字、二维码和 UI 数据均为示意，不得直接作为业务事实。

## 7. 默认技术基线

以下是 Blueprint 的推荐起点，不表示依赖已安装；创建代码前仍需检查实际仓库状态和任务约束。

- API：FastAPI。
- 异步任务：Python Worker + 可重试队列。
- 主数据库：PostgreSQL 17/18。
- 语义检索：pgvector，仅用于召回、去重和语义候选，不裁决硬资格。
- 对象存储：S3 兼容存储，保存 HTML/PDF/Excel/图片原件与快照。
- Redis：队列、缓存、锁和限流；绝不作为 Opportunity 事实源。
- 采集：httpx/DOM 解析优先，动态页面才使用 Playwright。
- 用户端：响应式 Web/PWA 优先，小程序复用同一 API 和事件模型。
- 模型：外部 LLM API 经模型网关（Model Gateway）访问；MVP 不购买 GPU、不训练基础模型、不绑定单一供应商。

### 7.1 AI 使用规则

- 优先写入时智能（Write-time Intelligence）：文档入库时解析一次，结构化事实供多用户复用。
- 日期、枚举、资格计算和大量匹配使用确定性组件；强模型只处理复杂自然语言、跨附件冲突和语义候选。
- 所有模型调用记录 provider、model、prompt、temperature、schema、成本和版本。
- 结构化输出必须通过 JSON Schema；高影响字段必须带证据定位。
- 模型升级先在 Golden Dataset 和模拟画像库进行离线与影子运行，比较差异后才能进入真人版本。
- 生产主链采用可恢复工作流和状态机。Agent 适合提出候选、处理长尾和辅助人工，不得成为不可审计的最终裁决者。

## 8. 数据、来源、隐私与合规

- Source 至少区分官方一手源、官方聚合源、可信二手线索和社区线索；硬资格最终证据优先来自官方一手源。
- 采集遵守公开使用规则、robots、合理频率和访问控制；不得绕过登录、验证码、付费墙或技术限制。
- 公开展示以必要事实、证据定位和官方回链为主，不无必要复制整站内容。
- 画像遵循最小必要、目的限定和渐进询问；允许跳过非必要字段。
- 预留查看、更正、删除、注销、推荐开关、解释、申诉、日志审计、权限隔离、备份与恢复能力。
- 不长期保存可由低敏信息计算出的高敏原始字段；优先保存业务所需派生结果。
- 用户不是可出售的数据资产；不得把画像、行为或反馈出售、共享给无关第三方，机构合作不得改变自然资格与排序。
- 面向公众的 AI 生成内容必须按适用要求标识。
- Blueprint 不是法律意见。公测前根据实际主体、功能、地区和规模由合格法律顾问确认义务。

## 9. 质量目标与发布门

以下均为计划阈值，必须由可复现的评估运行证明，不能凭主观判断宣布达标。

- 公开可信字段完整率：`100%`。
- 关键字段抽取准确率：`>=97%`。
- 注册目标源新机会发现覆盖率：`>=95%`。
- 高影响变更识别率：MVP `>=95%`，扩张目标漏检 `<=1%`。
- `INELIGIBLE` 误杀率：`<=0.5%`，严重错误零容忍复盘。
- 硬结论证据可追溯率：`100%`。
- 最小画像完成率：`>=60%`，且不能依靠强迫收集无关字段。
- 7 天未知可申请机会发现率：`>=30%` 形成信号，`>=50%` 才考虑扩区域。
- 活跃用户周高意图动作率：`>=15%`。
- 100 个官方源运行 14 天、无需日常人工维护：`>=90%` 才批量扩源。

六道发布门：

1. Gate A 数据可信：原文、归并、字段、公开可信层和源健康稳定。
2. Gate B 资格可信：不存在不可接受的错误否定或证据缺失。
3. Gate C 模拟稳健：画像与边界场景无系统性偏差。
4. Gate D 真人行动：公开浏览能进入体检，用户发现未知机会并产生真实行动。
5. Gate E 商业/扩张：用户为持续判断与监控产生真实付费或续约证据。
6. Gate F 合规扩张：面向公众或机构规模扩张前，按实际功能完成招聘服务、AI 内容标识、数据保护、内容使用和人工责任边界核验。

### 9.1 阶段 Engineering Gate 与 Release Qualification

阶段工程验收与真实环境发布资格必须分轴记录，不得再用一个 `Gate` 状态同时表达两者：

- 实现状态只使用 `PLANNED`、`IN_PROGRESS`、`IMPLEMENTED`。
- 工程门（Engineering Gate）只使用 `OPEN`、`CLOSED`。它判断本阶段实现、契约、迁移、测试、
  安全、评审和范围是否足以允许下一阶段开发；`CLOSED` 后，下一阶段不因本阶段 Release
  Qualification 尚未完成而形成级联阻塞状态。
- 发布资格（Release Qualification）独立使用 `NOT_STARTED`、`IN_PROGRESS`、`QUALIFIED`、
  `FAILED`、`BLOCKED`。每个阶段只采用自身基线适用的真实时间、真实环境、Gold Dataset、
  真人验证、新鲜副本或最终候选 CI 项目；这些项目阻塞正式生产发布，不阻塞下一阶段正常工程开发。
- 契约成熟度独立使用 `PROPOSED`、`IMPLEMENTED`、`STABLE`。实现已存在且 Engineering Gate
  已关闭时可标记 `IMPLEMENTED`；只有对应 Release Qualification 为 `QUALIFIED` 才可标记
  `STABLE`。
- Release Qualification 未完成或环境性失败不自动重开 Engineering Gate；若它暴露可复现的
  代码、契约、迁移或安全缺陷，则必须记录缺陷并重新评估受影响阶段的 Engineering Gate。
- 阶段间真实的代码、迁移和契约依赖仍须按顺序集成和兼容验证；禁止把技术依赖重新包装成
  `BLOCKED_BY_PHASE2` 或 `IMPLEMENTED_PENDING_*` 这类跨阶段状态链。

本节的阶段状态不替代上面的 Gate A–F 产品发布门。Gate A–F 仍按产品验证顺序执行，不得因
Engineering Gate 关闭而宣称真实数据、真人行动、商业或合规验证已经完成。

未通过前一道 Gate，不得用扩大地域、增加聊天、增加内容、营销投放或复杂 UI 掩盖问题。

## 10. 开发顺序与工作流

优先顺序：

`数据契约 -> 原始证据 -> Opportunity/Version -> 规则与资格 -> 评估回放 -> 公开可信层 -> 个人行动薄链路 -> 提醒/计划 -> 商业化`

不要先做漂亮的完整门户，再补数据与规则。

每个任务按以下步骤执行：

1. 阅读本文件和与任务直接相关的 Blueprint 章节。
2. 盘点现有代码、测试、配置和工作区改动；不要假设组件已经存在。
3. 明确假设、范围、失败模式和可执行验收标准。
4. 选择最小可验证纵向切片，列出将创建或修改的准确文件。
5. 功能/缺陷先建立失败测试或可复现样本，再写最小实现。
6. 运行定向测试，再运行与改动风险匹配的回归、静态检查、构建或端到端验证。
7. 检查数据证据、版本、审计、隐私和不确定性表达是否仍满足不变量。
8. 仅更新受改动影响的文档；清楚区分“计划”“实现”“验证”三种状态。
9. 交付时说明改了什么、如何验证、剩余风险和未做事项。

## 11. 测试与可复现性

- 解析器测试覆盖 HTML、PDF、Excel、图片/低置信度和跨附件冲突样本。
- Resolver 测试覆盖重复公告、附件归并、更正、延期、取消、稳定 ID、合并和撤销。
- Rule Engine 使用人工标注真值与确定性断言，重点覆盖专业、应届身份、年龄日期、学历、户籍、证书和边界条件。
- 资格测试对错误否定采取比一般排序错误更严格的门槛。
- 排序测试使用成对反事实，证明单一偏好变化只造成合理排序变化，不改变硬资格。
- LLM 测试默认使用固定输入、结构化契约、录制或模拟响应；常规测试不依赖实时模型网络调用。
- 每次评估保存数据集、机会快照、画像、规则、解析器、Prompt、模型、排序器和运行时间版本。
- 新版本依次经过组件评估、端到端回放、影子流量和发布门。
- UI 测试覆盖移动端、响应式布局、键盘可用性、清晰状态、证据入口、加载/空/失败/不确定状态。
- 至少保留一条端到端薄链路：官方原文 -> 结构化机会 -> 资格四态 -> 个人解释 -> 官方跳转/行动 -> 反馈入队。

## 12. 完成定义

任务只有同时满足以下条件才算完成：

- 结果满足明确的用户请求和验收标准，没有额外功能。
- 相关测试、检查、构建或回放已实际运行并通过。
- 高影响判断保留证据、版本和审计关系。
- 缺失或冲突信息被表达为不确定，不被模型或 UI 隐藏。
- 没有破坏隐私最小化、用户控制、官方回链或商业排序隔离。
- 文档没有把目标值写成已达成，也没有把规划写成已实现。
- 交付说明包含验证证据、剩余风险和明确未做范围。

## 13. 常见偏航信号

出现以下情况应停止扩展并回到核心闭环：

- 用公告数量、页面浏览量或视觉完成度代替机会价值。
- 用一个匹配百分比代替资格四态、证据和不确定性。
- 把聊天界面、大模型或多 Agent 编排当作产品护城河。
- 同一份公告被多次即时 LLM 解读，且没有持久化结构化事实。
- 模拟用户结果被当作真人信任、留存、付费或市场证明。
- 资格规则藏在 Prompt 中，无法执行、回归或审计。
- Redis、向量相似度或模型输出成为事实源或最终资格裁决。
- 未通过数据与资格 Gate 就扩全国、扩品类、收费或投放。
- 直接把 `设计/` 中除正式品牌基准外的海报、二维码、候选图标或示意数据作为生产资产，或未经清理就直接部署带白底/水印的 `logo.png` 原图。
