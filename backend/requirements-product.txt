# Tested direct dependencies for the rebuilt runtime; Python 3.13 / 3.14.
fastapi==0.128.2
starlette==0.50.0
uvicorn==0.48.0
SQLAlchemy==2.0.50
pydantic==2.13.4
httpx==0.28.1
cryptography==46.0.4
psutil==7.2.2
tzdata==2026.2
# 在线调查（WMA）客户端。import 名为 cloud_agent_sdk，与包名不同。
# 缺失时产品仍可正常运行，但 WMA 连接检查会显示"SDK：未安装"。
codebuddy-cloud-agent-sdk==0.3.4
openpyxl==3.1.5
et-xmlfile==2.0.0
