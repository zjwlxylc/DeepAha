# 下一项主系统任务：研究资产暂存与真实接收回执

先阅读用户提供的最新主仓、2026-09-12评估包中的08/13/15号文档，以及本工具v0.3.0的COMPATIBILITY、FEEDBACK_PLAN、schemas。

核对当前HEAD与输入ZIP comment d0918ec1245264e271f42a18d54c0fb903000c00；若已更新，只盘点相关接口差异，不覆盖当前工作。独立短期worktree执行。

本次只完成既有R2-A/B与R2-C的最小闭环：安全接收GPT/WB原件和research-handoff.v1；保留原件/版本/研究命名空间，呈现疑点；授权用户显式接收候选；短事务提交后由DeepAha数据库生成并可查询/导出的真实接收回执。旧Profile执行内核不能因研究包不兼容而放宽。复用现有对象存储、权限、审计和数据库；不要引入第二个Source Registry或生产SQLite。

验收使用本轮真实文件离线回放（30+18批，92+72观察，37+52身份组，33附件）；至少覆盖同包重传、同run异内容、事务回滚、并发revision、伪造PRODUCTION反馈、缺权限。接收后正式Source/Endpoint/Opportunity状态必须完全不变。新API路径和迁移编号以实际代码/数据库head为准，不按外部文档猜测。

不实施R2-D来源批准和R2-E真实发现，不启动WMA，不自动merge main或部署；完成后给出差异、真实测试、未验证项及由系统回读的接收记录。用户确认后再决定后续来源启用。
