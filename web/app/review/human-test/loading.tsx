export default function HumanTestLoading() {
  return (
    <main id="main-content" className="page-shell human-test-shell">
      <div className="route-state" role="status">
        <h1>正在读取本地人工测试状态</h1>
        <p>正在核对本地 reviewer、Provider 脱敏状态和可恢复运行记录。</p>
      </div>
    </main>
  );
}
